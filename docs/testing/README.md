# Testing

Unit/component tests live next to source files. Browser tests live in `e2e/`. See `verification-report.md` for the difference between checks executed in the delivery sandbox and gates still required after dependency installation.
