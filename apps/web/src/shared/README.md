# Shared layer

Contains stable, cross-feature contracts, server helpers and UI primitives. It must not contain GymSheet business workflows. Network access is restricted to `shared/api`, `shared/server` and authentication service boundaries.
