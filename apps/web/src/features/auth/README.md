# Authentication

Implements login and registration forms against same-origin `/api/auth/*` route handlers. The feature never receives or stores the backend access token. Logout clears the HttpOnly session cookie.
