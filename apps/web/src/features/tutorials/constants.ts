/** z-index for the tour layer — above the app header (40), dialogs (50) and the
 * route-progress bar (60). Kept in one place so it is easy to reason about. */
export const TUTORIAL_Z = 70;

/** Padding (px) added around a highlighted target when drawing the spotlight. */
export const SPOTLIGHT_PADDING = 6;

/** Route of the Tutorial Center (Centro de ayuda). */
export const TUTORIAL_CENTER_ROUTE = '/tutorials';
