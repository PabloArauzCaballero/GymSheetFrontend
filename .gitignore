node_modules
.next
out
dist
coverage
playwright-report
test-results

# Salida y material de las corridas E2E: las capturas y el vídeo/QR sintéticos
# se regeneran con `scripts/make-fake-*.mjs`, no se versionan (el Y4M pesa 23 MB).
e2e-evidence
e2e-evidence-theme
.e2e-assets
.turbo
.env
.env.local
# Copias de seguridad de .env (p. ej. al rotar la IP de LAN del backend): son
# locales y contienen la misma configuracion sensible que el original.
.env.bak.*
# Credenciales locales de las pruebas end-to-end.
.env.e2e
*.log
*.tsbuildinfo
.DS_Store

# Generated knowledge-graph output (graphify)
graphify-out

# Expo / React Native
apps/mobile/.expo
apps/mobile/android
apps/mobile/ios
apps/mobile/*.log
